<div class= "home">
	<div class= "title">
		<p class= "p_search">explore raspas entre <%= pluralize @tags.count, "tema" %></p>
	</div>
</br>
</br>
	<div class= "tag_word">
		<% @tags.each do |tag| %>
			<div class= "tag_index">
		  		<%= link_to tag.name, tag, class: "but round" %><p class= "p_tags">x <%= tag.taggings.count %></p>
			</div>  	
	  	<% end %>
	</div>
	<div class= "pagination_square">
		<%#= will_paginate @tags %>
	</div>
</div>